# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# GitHub Push Rule
Do NOT execute `git push` to GitHub unless the user explicitly requests or confirms it.
